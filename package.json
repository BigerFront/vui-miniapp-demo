{
  "name": "id-wallet-test",
  "version": "0.1.0",
  "description": "Test",
  "scripts": {
    "lint": "prettier --write ./**/*.{js,wxs,wxml,json}",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lanbery",
  "license": "MIT",
  "miniprogram": "dist",
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,wxs,wxml,wxss,json}": [
      "prettier --write",
      "git add"
    ]
  },
  "devDependencies": {
    "husky": "^4.3.8",
    "lint-staged": "^10.5.3",
    "prettier": "^2.2.1"
  },
  "dependencies": {}
}
